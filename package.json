{
  "name": "leave-payroll-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready MERN Leave and Payroll Management System with RBAC, payroll processing, leave approvals, audit logs, and dashboards.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm start --prefix backend",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
